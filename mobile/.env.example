# Calculator Game Environment Variables
# =====================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values below
# 3. NEVER commit .env to source control (it's in .gitignore)
#
# FOR PRODUCTION (EAS Build):
# Use EAS Secrets instead of .env files:
#   eas secret:create --name FIREBASE_API_KEY --value "your-key"
#   eas secret:create --name FIREBASE_AUTH_DOMAIN --value "your-domain"
#   eas secret:create --name FIREBASE_DATABASE_URL --value "your-url"
#   eas secret:create --name FIREBASE_PROJECT_ID --value "your-project-id"
#   eas secret:create --name GEMINI_API_KEY --value "your-key"
#
# See: https://docs.expo.dev/build-reference/variables/

# Firebase Configuration
# Get these from: Firebase Console > Project Settings > General > Your apps
FIREBASE_API_KEY=your_firebase_api_key_here
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com
FIREBASE_PROJECT_ID=your-project-id

# Gemini API Configuration
# Get this from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Sentry Configuration (Optional - for error tracking)
# Get this from: Sentry Dashboard > Project Settings > Client Keys
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
